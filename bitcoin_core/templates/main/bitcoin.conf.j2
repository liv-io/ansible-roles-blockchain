#-------------------------------------------------------------------------------
#                             ANSIBLE MANAGED FILE
#-------------------------------------------------------------------------------

#-------------------------------------------------------------------------------
# INFORMATION
#-------------------------------------------------------------------------------
#
# Name   : bitcoin.conf
# Type   : bitcoind configuration file
# Role   : bitcoin_core
#
# Authors: liv community
# License: Simplified BSD License
#
#-------------------------------------------------------------------------------

# Generated by https://jlopp.github.io/bitcoin-core-config-generator/

# This config should be placed in following path:
# ~/.bitcoin/bitcoin.conf

# [core]
# Specify a non-default location to store blockchain data.
# Assume that this block hash and its ancestors are valid. Skip their script verification (performance boost.) Set to 0 to verify all blocks. We recommend leaving value blank.
assumevalid=0
# Specify the location of the configuration file. To use non-default location, create a default location config file containing this setting.
conf={{file_dst_bitcoin_conf}}
# Run in the background as a daemon and accept commands.
daemon=1
# Specify a non-default location to store blockchain and other data.
datadir={{file_dst_bitcoind_data_dir}}
# Set database cache size in megabytes; machines sync faster with a larger cache. Recommend setting as high as possible based upon machine's available RAM.
dbcache=2048
# Specify process ID file name. Ignored for Windows.
pid={{file_dst_bitcoind_data_dir}}/bitcoind.pid
# Maintain a full transaction index, used by the getrawtransaction rpc call.
txindex=1

# [network]
# Bind to given address and always listen on it. Use [host]:port notation for IPv6.
bind=0.0.0.0
# Support filtering of blocks and transactions with bloom filters.
peerbloomfilters=0
# Listen for incoming connections on non-default port.
port=8333
# Only connect to peers via IPV4
onlynet=ipv4

# [zeromq]
# Enable publishing of block hashes to <address>.
zmqpubhashblock=tcp://127.0.0.1:5555
# Enable publishing of transaction hashes to <address>.
zmqpubhashtx=tcp://127.0.0.1:5555
# Enable publishing of raw block hex to <address>.
zmqpubrawblock=tcp://127.0.0.1:5555
# Enable publishing of raw transaction hex to <address>.
zmqpubrawtx=tcp://127.0.0.1:5555


# [debug]
# Disable expensive verification for known chain history.
checkpoints=0
# Location of the debug log
debuglogfile={{file_dst_bitcoind_log_dir}}/debug.log
# Send trace/debug info to console instead of debug.log.
printtoconsole=1

# [rpc]
# Accept command line and JSON-RPC commands.
server=1
# Bind to given address to listen for JSON-RPC connections. This option is ignored unless -rpcallowip is also passed. Port is optional and overrides -rpcport. Use [host]:port notation for IPv6. This option can be specified multiple times. (default: 127.0.0.1 and ::1 i.e., localhost, or if -rpcallowip has been specified, 0.0.0.0 and :: i.e., all addresses)
rpcbind={{bitcoin_core_rpc_bind}}
# Accept public REST requests.
rest=1
# Listen for JSON-RPC connections on this port
rpcport=8332
# Username and hashed password for JSON-RPC connections. The field <userpw> comes in the format: <USERNAME>:<SALT>$<HASH>. RPC clients connect using rpcuser=<USERNAME>/rpcpassword=<PASSWORD> arguments. You can generate this value with the ./share/rpcauth/rpcauth.py script in the Bitcoin Core repository. This option can be specified multiple times.
{% if not (bitcoin_core_rpc_auth == '') %}
rpcauth={{bitcoin_core_rpc_auth}}
{% endif %}
# Allow JSON-RPC connections from specified source. Valid for <ip> are a single IP (e.g. 1.2.3.4), a network/netmask (e.g. 1.2.3.4/255.255.255.0) or a network/CIDR (e.g. 1.2.3.4/24). This option can be specified multiple times.
rpcallowip={{bitcoin_core_rpc_allow_ip}}

# [wallet]
# Do not load the wallet and disable wallet RPC calls.
disablewallet=1


# [Sections]
# Most options automatically apply to mainnet, testnet, and regtest networks.
# If you want to confine an option to just one network, you should add it in the relevant section.
# EXCEPTIONS: The options addnode, connect, port, bind, rpcport, rpcbind and wallet
# only apply to mainnet unless they appear in the appropriate section below.

# Options only for mainnet
[main]

# Options only for testnet
[test]

# Options only for regtest
[regtest]
